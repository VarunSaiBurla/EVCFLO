const Hero = ({text}) => {
    return(
        <div className=" w-full bg-slate-100 rounded-xl py-12 font-medium text-base container flex justify-center h-1/4">
            {text}
        </div>
    )
}

export default Hero;